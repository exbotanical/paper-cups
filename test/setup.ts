/* eslint-disable jest/require-top-level-describe */

beforeEach(() => {
	jest.resetAllMocks();
	jest.clearAllMocks();
	jest.clearAllTimers();
});

afterEach(async () => {
	jest.resetAllMocks();
	jest.clearAllMocks();
	jest.clearAllTimers();
});
